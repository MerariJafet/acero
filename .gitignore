# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# ACERO runtime state (local-first data is not committed)
*.sqlite
*.sqlite3
*.db
acero_data/
research/artifacts/*/
!research/artifacts/.gitkeep
# Downloaded datasets (reproducible via world_model.ingest.download_exoplanets)
research/datasets/*.csv

# Environment / secrets — NEVER commit real secrets
.env
.env.local
*.key
*.pem

# Editors / OS
.vscode/
.idea/
.DS_Store

# Node (future dashboard)
node_modules/
research/artifacts/*.html
backups/
scratch_*
research/cache/

# ACERO runtime / user data (nunca al repo)
data/
research/loop/
research/selfimprove/
experiments/
script.py
research/calibration.json

# checkpoints de cómputo largo (cover_growth.ckpt pesa ~460 MB: es estado reanudable, no fuente)
research/reto50/*.ckpt
