# Preserved negative results

- The recovered transit is a KNOWN planet (Kepler-8b). Recovery is not discovery.
- In the full ACERO program, AR(1) red-noise and inverted-transit null tests were
  NOT controlled by the naive BLS-SNR pipeline (false-positive rate ~0.4), so the
  Abstention Engine ABSTAINED from even the bounded claim.
- Injected signals are recovered as a TEST of the pipeline; they are not
  astrophysical observations.
- Two/three methods over the SAME data are not independent replication.
